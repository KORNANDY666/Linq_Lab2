@model IEnumerable<Linq_Lab2.Models.SchoolClass>

@{
    ViewData["Title"] = "Index";
}

<h1>Classes</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.StudentClass)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.StudentClass)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.SchoolClassId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.SchoolClassId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.SchoolClassId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
